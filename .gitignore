# Dependencies
/node_modules/

# Safety-critical backup and local data protections
backups/
/backups/
backups/manual/
/backups/manual/
backups/**/*.sql
/backups/**/*.sql
backups/**/*.tar.gz
/backups/**/*.tar.gz
backups/**/*.sha256
/backups/**/*.sha256
*.sql
*.dump
*.backup
.env
.env.*
local-uploads/
/local-uploads/
uploads/
/uploads/
provider-documents/
/provider-documents/
client-documents/
/client-documents/

# Local runtime uploads
/uploads/
/data/

# Supabase manual backup outputs (sensitive)
/backups/manual/

# SQL dump artifacts (never commit)
roles.sql
schema.sql
data.sql
*.dump
*.sql.gz

# Local environment files and secrets
.env
.env.*
*.env
*.secret
*.secrets
secrets/

# Certificate and private key material
*.pem
*.key
*.p12
*.pfx

# Backup readiness artifacts (sensitive)
/backups/automated/
/backups/**/*.tar.gz
/backups/**/*.sha256
